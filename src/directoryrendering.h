#ifndef DIRECTORY_RENDERING
#define DIRECTORY_RENDERING

// #include "uirendering.h"
#include "raylib.h"

void updateInsidePane(int *indexFileSelected,
                      char **fileNames, bool *musicPlaying, Music *music,
                      int *numFile);
#endif
